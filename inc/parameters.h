#pragma once
#include <raylib.h>

// Windows parameters
const int widthNbrCells = 50;
const int heightNbrCells = 20;
const int screenWidth = 800;
const int screenHeight = 600;
const char *screenTitle = "My first RAYLIB program!";
const int FPS = 60;
const Color initBackground = BLACK;

// Cells parameters
const int cellSize = 20;
const int wallThickness = 2;
